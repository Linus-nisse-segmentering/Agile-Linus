global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "backend"
    static_configs:
      # Prometheus runs on the backend VM and shares a Docker network with the app.
      - targets: ["app:1010"]
    metrics_path: /metrics
